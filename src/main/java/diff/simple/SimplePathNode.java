package diff.simple;

import diff.OperationValue;
import diff.PathNode;

/**
 * 用于记录路径的节点
 */
public class SimplePathNode extends PathNode {
    public SimplePathNode(OperationValue opv) {
        super(opv);
    }
}
