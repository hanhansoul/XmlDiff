package maindiff.xml.output;

import maindiff.abs.output.PathNode;
import maindiff.abs.work.OperationValue;

// TODO XmlPathNode
public class XmlPathNode extends PathNode {
    public XmlPathNode(OperationValue opv) {
        super(opv);
    }
}
