package maindiff.simple.output;

import maindiff.abs.output.OutputNode;
import maindiff.abs.work.Node;

public class SimpleOutputNode extends OutputNode {

    public SimpleOutputNode(Node node, boolean isEndTag) {
        super(node, isEndTag);
    }
}
